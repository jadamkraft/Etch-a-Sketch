/* javascript.js */

document.addEventListener("load", addElement);

function addElement() {
    const contentDiv = document.createElement("div");
    const rowOne = contentDiv.className = "rowOne";
    contentDiv.appendChild(rowOne);
    div.container.appendChild(contentDiv);
}